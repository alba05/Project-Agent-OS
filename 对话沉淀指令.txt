请不要继续原任务，先执行一次“对话沉淀”，并把结果直接写入本地文件夹：

`C:\Users\Administrator\Documents\Project-Agent-OS\examples\sample-assets\thread-summaries\`

文件名规则：
`dialog-<当前会话ID>-<开始时间>-<结束时间>.md`

时间格式规则：
`YYYYMMDD-HHMM`

开始时间：
本次沉淀实际覆盖内容的起始时间

结束时间：
本次沉淀执行时的当前时间

例如：
`dialog-abc123-20260622-0410-20260622-0655.md`

请基于当前这个会话已经发生过的全部上下文，生成一份结构化摘要，并直接写入该文件。不要等我再次操作，不要让我手动复制整理。写入完成后，再在聊天里只用短回复告诉我：

1. 写入的完整文件路径
2. 一句 50 字以内的摘要结论

写入文件内容时，请严格使用下面结构：

# 对话摘要：<当前会话ID>

## 覆盖时间范围
写明本次沉淀覆盖的开始时间和结束时间。

## 主题
用一句话概括这个会话主要在推进什么。

## 已完成事项
用 5-10 条列出这个会话里已经完成的关键动作、产出、下载、整理或决策。

## 当前稳定结论
总结这个会话里已经收敛、后续可以复用的规则。只写稳定结论，不写还在摇摆的想法。

## 未完成 / 下一步
列出这个会话如果以后继续，最自然的后续动作是什么。

## 可用于新对话承接的一段话
写一段 150-300 字左右的连续文字，能让新对话快速承接当前进展。

要求：
- 不要继续原任务
- 不要长篇复述过程
- 不要补新方案
- 只提炼稳定结论
- 默认面向 `Project-Agent-OS/examples/sample-assets` 这一层
- 重点沉淀 AI 音乐生成、资产下载、文件归档、元数据整理相关经验
- 不要改主仓库 `README` 或 `docs/system/`
- 如果发现当前会话信息不足，也先尽量写出可确认部分，不要空着
- 如果目录里已经存在同一会话较早时间段的沉淀文件，请先读取最新一份，避免把已沉淀过的时间范围重复再写一遍
- 新沉淀默认只覆盖“上一次沉淀结束时间之后”新增的内容；如果必须重做完整沉淀，要在摘要中明确写“本次为全量重整”
- 不要覆盖旧沉淀文件；一律新增带时间范围的新文件，保留时间线
- 如果发现旧沉淀里的“未完成 / 下一步”已经在当前会话中完成，请在本次新沉淀里把它转移到“已完成事项”，不要重复保留为待办
- 如果发现旧沉淀里的待办仍未完成，但优先级或表述发生变化，请在本次新沉淀里更新后的“未完成 / 下一步”中重写，不要原样复制
